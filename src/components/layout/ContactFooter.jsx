import React from "react";
import styles from "./ContactFooter.module.css";

const ContactFooter = () => {
  return (
    <footer className={styles.footer}>
      <h4>It wont fly unless someone pushes the button.</h4>
    </footer>
  );
};

export default ContactFooter;
